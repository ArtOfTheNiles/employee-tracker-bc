import colors from "colors";

export const comingSoon = console.log(colors.bgRed('FUNCTION IMPLEMENTATION INCOMING...'));
export const tempTelemetry = console.log(colors.gray('v v v   This is when you should see:   v v v'));
